<div class="max-w-2xl mx-auto my-10 bg-white p-6 rounded-lg shadow">
    <img src="<%= menu.image %>" alt="<%= menu.name %>" class="w-full h-64 object-cover rounded-md mb-4">
    <h2 class="text-2xl font-bold mb-4"><%= menu.name %></h2>
    <p class="text-gray-600 mb-4"><%= menu.description %></p>
    <p class="text-2xl font-bold text-gray-800">$<%= menu.price %></p>
    <a href="/menu" class="text-blue-500">Back to Menu</a>
</div>
